# access.cfg — per model: which departments may READ it, and per
# department, which input measures they may WRITE. Write grants reach
# only literal input cells — formulas are admin-only, structurally.
model industrial_budget.fml
  departments finance sales
  write sales: volume price
  write finance: *
