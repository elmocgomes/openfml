# users.cfg — one line per person:  <user>: <department> <role>
# Roles: admin (finance/HQ — formulas, process, everything),
#        editor (their department's granted input cells only),
#        viewer (read-only).
alice: finance admin
bob: sales editor
